# Native call sweep across the Rust tools

Status: survey, written 2026-09-20. No Rust changed, no dependency added, no apply ran and no
notification was delivered to produce it. This is the second instalment of the operator's 2026-09-19
prefer-native ruling: task 144 took the three probes the
`docs/superpowers/specs/2026-09-17-native-macos-probe-evaluation.md` measurements recommended (merged
`d2a3f36f1`), and this document sweeps every other command the four shipped tools spawn.

Method: `grep -rn "Command::new("` plus a grep for every absolute program path and every configured
program constant across `pns/crates`, `uu/crates`, `posture/crates` and `lights/crates`, with test
modules and fixtures excluded. Fifty-seven distinct commands survive that filter, in the fifty table rows
below: the five `sudo` file utilities share one row, and so do the four skills-lane installers. Every
native claim below was checked against the headers in `$(xcrun --show-sdk-path)` or against the shipped
`libc` 0.2.189 source in the local cargo registry; anything that could not be checked that way is marked
UNVERIFIED in its own row.

Finding in one line: **posture is where the money is.** Nine of its commands have a native answer, six of
those are one call each, and its poller pays for them on every tick. pns is already done (task 144 took
everything worth taking, and the rest of its spawns are other people's CLIs by design), uu is almost
entirely product CLIs whose job is to be those CLIs, and lights has exactly one: a `gtimeout` wrapper
that a bounded wait in process replaces.

## How to read the columns

- **Deadline** answers whether the replacement can block, and therefore whether it needs the
  interruptible wrapper. Every shelled command today runs under a deadline enforced from outside the
  process. A native call in process has no such bound, so an adoption either goes through a wrapper like
  pns's `bounded_call` (a thread plus `recv_timeout`, in
  `pns/crates/pns-adapters/src/process/bounded.rs`) or it accepts an unbounded call. `posture` has no
  such wrapper yet and may not borrow pns's, because no workspace may depend on another, so the first
  posture adoption carries its own copy.
- **Saving** is per call, from the orders of magnitude the 2026-09-17 measurements established: a spawn
  costs tens of milliseconds (the bare `/usr/bin/true` floor alone measured 1.62 ms, and a real probe 4
  ms to 45 ms), an in-process read costs microseconds. Nothing below was timed again for this survey.
- **Verdict** is one of replace, keep or defer. "Keep" for a product whose job is another CLI (`git`,
  `gh`, `herdr`, `brew`) is not a concession: the tool is the interface, and reimplementing it is the
  opposite of the ruling.

## pns

Everything the 2026-09-17 evaluation recommended has landed, so the remaining pns rows are either
adjudicated keeps or other people's programs.

| Command                                         | Where                                                            | Why it is spawned                            | Native replacement                                                                                                                                                                                                                                                                                                      | Deadline                  | Saving              | Verdict                                                                                                                                                          |
| ----------------------------------------------- | ---------------------------------------------------------------- | -------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pgrep -x mosh-server`                          | `pns-adapters/src/macos/phone.rs`                                | the detached mosh servers                    | a libproc name walk, already written next door in `proc_table.rs`                                                                                                                                                                                                                                                       | would need `bounded_call` | ~26 ms              | keep: adjudicated 2026-09-17, macOS `pgrep` matches a name field `proc_name` does not reproduce exactly, and a phone selected differently is a different reading |
| `launchctl {bootstrap,bootout,kickstart,print}` | `pns-adapters/src/macos/launchd.rs`                              | `pns gateway start/stop/restart/status`      | none. `launch.h` states it itself: "There are currently no replacements for other uses of the `launch_msg` API, including submitting, removing, starting, stopping and listing jobs". `ServiceManagement`'s `SMAppService` reports four registration states and no run count or last exit code, and it is bundle-shaped | n/a                       | n/a                 | keep, verified against the SDK header                                                                                                                            |
| `open -na Ghostty.app --args -e <cmd>`          | `pns-domain/src/failure/click.rs`                                | a click opens the record in a new window     | `LSOpenApplicationAtURL` / `NSWorkspace.openApplication` in ApplicationServices                                                                                                                                                                                                                                         | would need a wrapper      | one spawn per click | defer: a click is operator-paced, the call runs in a bare launchd context, and the AppKit path wants a bundle and a run loop                                     |
| `herdr pane split --command ...`                | `pns-domain/src/failure/click.rs`                                | a click opens the record in a herdr pane     | none                                                                                                                                                                                                                                                                                                                    | n/a                       | n/a                 | keep: herdr's CLI is the interface                                                                                                                               |
| `git` (rev-parse, status, log, diff)            | `pns-adapters/src/git.rs`, `recap/worktree.rs`                   | the recap's Git block                        | none worth having                                                                                                                                                                                                                                                                                                       | n/a                       | n/a                 | keep: a git library is a bigger dependency than the CLI, and the CLI is the contract the recap was measured against                                              |
| `gh`                                            | `pns-adapters/src/recap/github_cli.rs`                           | the recap's pull-request numbers and states  | the GitHub REST API over HTTPS                                                                                                                                                                                                                                                                                          | n/a                       | one spawn per recap | keep: the operator's 2026-09-19 ruling keeps `gh` inside a shipped product precisely because it carries its own credentials                                      |
| `moshi-hook`                                    | `pns-adapters/src/moshi_hook.rs`, `process/settings.rs`          | the presence-gated pass-through              | none                                                                                                                                                                                                                                                                                                                    | n/a                       | n/a                 | keep: forwarding to moshi-hook is the feature                                                                                                                    |
| `codex`                                         | `pns-adapters/src/codex.rs`                                      | asks Codex its own question                  | none                                                                                                                                                                                                                                                                                                                    | n/a                       | n/a                 | keep                                                                                                                                                             |
| the configured calendar command                 | `pns-adapters/src/calendar.rs`                                   | one JSON document of events                  | none                                                                                                                                                                                                                                                                                                                    | n/a                       | n/a                 | keep: the producer contract is the point, and no calendar is named in pns                                                                                        |
| the configured summarizer                       | `pns-adapters/src/recap/summarizer.rs`                           | the recap's prose                            | none                                                                                                                                                                                                                                                                                                                    | n/a                       | n/a                 | keep                                                                                                                                                             |
| the configured destination channel              | `pns-adapters/src/destinations/executable.rs`                    | a channel the config names                   | none                                                                                                                                                                                                                                                                                                                    | n/a                       | n/a                 | keep                                                                                                                                                             |
| itself, via `current_exe`                       | `daemon_children.rs`, `recap_child.rs`, `process/shell_event.rs` | the daemon's children and the detached event | none: a re-exec is the isolation                                                                                                                                                                                                                                                                                        | n/a                       | n/a                 | keep                                                                                                                                                             |

## posture

Nine replaceable commands, and the poller pays for several of them on every tick.

| Command                                                                  | Where                                                          | Why it is spawned                                              | Native replacement                                                                                                                                                                                                                                                                                                       | Deadline                            | Saving                              | Verdict                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------ | -------------------------------------------------------------- | -------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `pgrep -P 1 -x osqueryd`                                                 | `posture-adapters/src/converge/parents.rs`                     | the daemon's parent, to prove a restart re-parented it         | a libproc walk: `proc_listpids(PROC_ALL_PIDS)` then `proc_pidinfo(PROC_PIDTBSDINFO)`, comparing `pbi_ppid` and `pbi_name`. All declared in `libc` 0.2.189 (`proc_listpids` at `unix/bsd/apple/mod.rs:4987`, `proc_pidinfo` at 4992, `proc_name` at 5014); the public SDK declares the same in `libproc.h:92`, `96`, `99` | yes, wrap it                        | ~26 ms                              | replace. The parent id is an integer comparison, which carries none of the name-selection risk the mosh row does; the name half keeps the `pbi_name` caveat                                                               |
| `pgrep -fq '/opt/osquery/.*osqueryd'`                                    | `posture-adapters/src/watchdog_processes.rs`                   | is osqueryd running at all                                     | the same walk plus `proc_pidpath` (declared at `libc` `apple/mod.rs:5013` and `libproc.h:102`) for the path half, or `sysctl` with `KERN_PROCARGS2` (`libc` const at `apple/mod.rs:3742`) for a true full-command-line match                                                                                             | yes                                 | ~26 ms                              | replace, but SECOND: `-f` matches the whole command line and `proc_pidpath` matches the executable path, which is a narrower question. Pin the predicate against a real osqueryd before swapping                          |
| `pgrep -x -U <uid> <name>` (OverSight, the LuLu extension)               | `posture-adapters/src/probes.rs`                               | is the security agent alive                                    | the same walk, filtering `pbi_uid` and the name                                                                                                                                                                                                                                                                          | yes                                 | ~26 ms each, two controls           | replace, in the same slice as the first row                                                                                                                                                                               |
| `kill -0 <pid>`                                                          | `posture-adapters/src/watchdog_processes.rs`                   | does the pns daemon pid still exist                            | `libc::kill(pid, 0)`, declared at `unix/mod.rs:1211`                                                                                                                                                                                                                                                                     | cannot block                        | ~2 ms                               | replace: one line, no unsafe beyond the call, exactly the shape of the `getuid` call already in that file                                                                                                                 |
| `plutil -convert xml1 -o -`                                              | `posture-adapters/src/probes/lulu.rs`                          | the LuLu profile and rule archives as XML to grep              | read the file and parse it in process: `CFPropertyListCreateWithData` (`CFPropertyList.h:131`, available since 10.6) over the bytes, or the pure-Rust `plist` crate                                                                                                                                                      | yes for a huge file                 | ~5 ms to 20 ms, several per tick    | replace. The reading also gets better: today the code greps `<key>currentProfile</key>` out of rendered XML, and a parsed property list answers the key directly                                                          |
| `plutil -extract <key> raw -o -`                                         | `posture-adapters/src/codesign.rs`                             | one value out of a bundle's Info.plist                         | the same parsed property list                                                                                                                                                                                                                                                                                            | yes                                 | ~5 ms per enriched path             | replace, same slice                                                                                                                                                                                                       |
| `readlink -f <path>`                                                     | `posture-adapters/src/probes/lulu.rs`                          | resolve a LuLu rule's launcher symlink                         | `std::fs::canonicalize`                                                                                                                                                                                                                                                                                                  | no                                  | ~3 ms                               | replace: stdlib, no framework, no unsafe                                                                                                                                                                                  |
| `xattr -p com.apple.quarantine <path>`                                   | `posture-adapters/src/codesign.rs`                             | is the binary quarantined                                      | `libc::getxattr` (declared at `apple/mod.rs:4725`; the SDK's `sys/xattr.h:65` gives the same signature). `XATTR_NOFOLLOW` is available for the link question                                                                                                                                                             | no                                  | ~4 ms per path                      | replace                                                                                                                                                                                                                   |
| `file <path>`                                                            | `posture-adapters/src/codesign.rs`                             | is the path a Mach-O object                                    | read the first four bytes and compare the magic (`0xfeedfacf`, `0xfeedface`, `0xcafebabe` and their byte-swapped twins), which is all the current code does with the output anyway (it looks for "mach-o" in the text)                                                                                                   | no                                  | ~5 ms per path                      | replace: stdlib read, and it stops depending on `file`'s wording                                                                                                                                                          |
| `codesign -dv --verbose=2`                                               | `posture-adapters/src/codesign.rs`                             | the signing identity of a watched binary                       | `SecStaticCodeCreateWithPath` (`SecStaticCode.h:79`) then `SecCodeCopySigningInformation` with `kSecCSSigningInformation` (`SecCode.h:333`, `470`)                                                                                                                                                                       | yes, a signature check touches disk | ~50 ms or more per path, unmeasured | defer: the framework path is verified to exist, but the enrichment currently stores `codesign`'s own text and a structured dictionary is a different record. Needs a parity measurement and a record-shape decision first |
| `defaults read /Library/Preferences/com.apple.loginwindow autoLoginUser` | `posture-adapters/src/probes.rs`                               | is automatic login configured                                  | `CFPreferencesCopyValue` (`CFPreferences.h:89`) for `autoLoginUser` in `com.apple.loginwindow` with `kCFPreferencesAnyUser`, or simply the parsed property list from the plutil slice                                                                                                                                    | yes                                 | ~15 ms per tick                     | replace, once the property-list reader exists. UNVERIFIED: which user and host domain constants map to that exact file was reasoned from the header's own domain description, not measured                                |
| `fdesetup status`                                                        | `posture-adapters/src/probes.rs`                               | FileVault on or off                                            | none found. No public framework header in the SDK answers full-disk-encryption state                                                                                                                                                                                                                                     | n/a                                 | n/a                                 | keep. UNVERIFIED as an absence: a header search found nothing, which is weaker than a documented "no API"                                                                                                                 |
| `csrutil status`                                                         | `posture-adapters/src/probes.rs`                               | System Integrity Protection on or off                          | `csr_check` is the call, and `sys/csr.h` is NOT in the public SDK (checked: the file does not exist under `$(xcrun --show-sdk-path)/usr/include/sys/`), so it is a private interface                                                                                                                                     | n/a                                 | n/a                                 | keep, verified: a private SPI is not a native call a shipped product may take                                                                                                                                             |
| `sysadminctl -guestAccount status`                                       | `posture-adapters/src/probes.rs`                               | is the guest account enabled                                   | OpenDirectory, or a preferences read of the login window domain                                                                                                                                                                                                                                                          | yes                                 | ~20 ms                              | defer: no public call was verified for this one, and guessing at a preference key would change what the control means                                                                                                     |
| `sudo -n install -d/-m ...`                                              | `posture-adapters/src/converge/install.rs`                     | install our six files into `/var/osquery` as root              | none: a native write cannot gain privilege, and the whole point is that `sudo` is the escalation                                                                                                                                                                                                                         | n/a                                 | n/a                                 | keep                                                                                                                                                                                                                      |
| `sudo tee`, `sudo chmod`, `sudo cp`, `sudo mv`, `sudo rm`                | `posture-adapters/src/ssh_commands/files.rs`                   | stage, permission, back up, publish and clean the sshd drop-in | none, same reason: each is the program `sudo` runs                                                                                                                                                                                                                                                                       | n/a                                 | n/a                                 | keep (five rows, one reason)                                                                                                                                                                                              |
| `osascript`                                                              | `posture/src/{funnel,poll,alert,digest,heartbeat,watchdog}.rs` | the last-resort banner                                         | `UserNotifications` needs a signed bundle with a notification entitlement, which a `cargo install` binary does not have                                                                                                                                                                                                  | n/a                                 | n/a                                 | keep                                                                                                                                                                                                                      |
| `osqueryi`                                                               | `posture/src/{poll,allowlist}.rs`                              | the detection engine itself                                    | none                                                                                                                                                                                                                                                                                                                     | n/a                                 | n/a                                 | keep                                                                                                                                                                                                                      |
| `sshd -G -T -C ...`                                                      | `posture/src/ssh/configuration.rs`                             | render the effective sshd config to verify a drop-in           | none: only sshd can say what sshd will do                                                                                                                                                                                                                                                                                | n/a                                 | n/a                                 | keep                                                                                                                                                                                                                      |
| `ssh-keyscan`                                                            | `posture/src/ssh/configuration.rs`                             | prove a real banner exchange after a reload                    | none worth having                                                                                                                                                                                                                                                                                                        | n/a                                 | n/a                                 | keep                                                                                                                                                                                                                      |
| `tailscale status`-shaped read                                           | `posture/src/funnel.rs`                                        | is a funnel exposed                                            | the tailscaled LocalAPI over its unix socket                                                                                                                                                                                                                                                                             | yes                                 | ~30 ms per tick                     | defer: the LocalAPI is not a documented stable interface, and the CLI is                                                                                                                                                  |
| `chezmoi source-path`                                                    | `posture-adapters/src/publisher.rs`                            | find the manifest refresh script                               | none                                                                                                                                                                                                                                                                                                                     | n/a                                 | n/a                                 | keep                                                                                                                                                                                                                      |
| `bash <manifest script>`                                                 | `posture-adapters/src/publisher.rs`                            | refresh the known-good manifests                               | none: it is a shell script                                                                                                                                                                                                                                                                                               | n/a                                 | n/a                                 | keep                                                                                                                                                                                                                      |
| `launchctl print gui/<uid>/<label>`                                      | `posture-adapters/src/watchdog_processes.rs`                   | run count, last exit code, pid and state per agent             | none, same header evidence as the pns row                                                                                                                                                                                                                                                                                | n/a                                 | n/a                                 | keep, verified                                                                                                                                                                                                            |

## uu

uu is a lane runner whose lanes are other people's updaters, so almost every row is a keep by
construction. Two rows are not.

| Command                                                        | Where                                                     | Why it is spawned                                                     | Native replacement                                                      | Deadline | Saving                                                         | Verdict                                                                                                                                         |
| -------------------------------------------------------------- | --------------------------------------------------------- | --------------------------------------------------------------------- | ----------------------------------------------------------------------- | -------- | -------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------- |
| `/usr/bin/env VAR=... <program>`                               | `lanes/nvim/smoke_test.rs`, `lanes/skills/forks/clone.rs` | set environment variables for a child                                 | `Command::env`, through one new method on uu's own `CommandRunner` port | no       | one spawn per call, plus the argv-shaped environment goes away | replace: the spawn exists only because the port takes a program and arguments and no environment                                                |
| `/bin/sh -c '<PATH prepend>' ...`                              | `lanes/npm.rs`                                            | put the fnm directory first on the child's PATH                       | the same `Command::env` on the port                                     | no       | one spawn per npm lane step                                    | replace, same slice: the careful `${PATH:+:$PATH}` shell text becomes a string join in Rust, and the quoting question disappears with the shell |
| `cmp -s <a> <b>`                                               | `lanes/brew/repairs.rs`                                   | did `brew upgrade` move the tailscaled binary under the system daemon | a streaming byte compare in Rust                                        | no       | ~4 ms once a week                                              | keep: the code comment already chose `cmp` deliberately over reading two binaries into the process, and the value is one spawn per week         |
| `sudo -n tailscaled install-system-daemon`                     | `lanes/brew/repairs.rs`                                   | reinstall the root daemon after an upgrade                            | none, privileged                                                        | n/a      | n/a                                                            | keep                                                                                                                                            |
| `brew`, `mas`                                                  | `lanes/brew.rs`                                           | the upgrade lane                                                      | none                                                                    | n/a      | n/a                                                            | keep                                                                                                                                            |
| `uv`                                                           | `lanes/uv.rs`                                             | the uv tool lane                                                      | none                                                                    | n/a      | n/a                                                            | keep                                                                                                                                            |
| `cargo`                                                        | `lanes/cargo.rs`                                          | the cargo install lane                                                | none                                                                    | n/a      | n/a                                                            | keep                                                                                                                                            |
| `rustup`                                                       | `lanes/rustup.rs`                                         | the toolchain lane                                                    | none                                                                    | n/a      | n/a                                                            | keep                                                                                                                                            |
| `herdr`                                                        | `lanes/herdr.rs`                                          | herdr and its plugins                                                 | none                                                                    | n/a      | n/a                                                            | keep                                                                                                                                            |
| `npx`, `clawhub`, `cua-driver`, the configured routing command | `lanes/skills/*`                                          | the three skills fan-out lanes                                        | none                                                                    | n/a      | n/a                                                            | keep                                                                                                                                            |
| `git`                                                          | `lanes/skills/forks/clone.rs`                             | clone a fork to watch it drift                                        | none                                                                    | n/a      | n/a                                                            | keep                                                                                                                                            |
| `nvim`                                                         | `lanes/nvim/smoke_test.rs`                                | prove the config still loads                                          | none: the point is to run the real editor                               | n/a      | n/a                                                            | keep                                                                                                                                            |

## lights

| Command                                              | Where                                 | Why it is spawned                              | Native replacement                                                                                                                                                                                                                               | Deadline           | Saving                                                  | Verdict                                                                                                                                                           |
| ---------------------------------------------------- | ------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------ | ------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `gtimeout --foreground --signal=KILL <secs> pns ...` | `lights-adapters/src/notification.rs` | bound the `pns send` it makes for every action | a bounded wait in process: spawn `pns` directly, poll `try_wait` against a deadline and `kill` on overrun. Both sibling tools already do exactly this (`pns`'s `run_bounded`, `morning`'s `capture`), and `libc::kill` is the only call under it | it IS the deadline | one spawn per announcement, and one Homebrew dependency | replace: `gtimeout` is an unqualified name resolved off PATH, so a lights installed by `cargo install` on a machine without GNU coreutils silently bounds nothing |
| `~/.cargo/bin/pns send ...`                          | `lights-adapters/src/notification.rs` | raise the banner through the engine            | none                                                                                                                                                                                                                                             | n/a                | n/a                                                     | keep: spawning the producer is the documented integration, and lights may not link pns                                                                            |

## Not in scope, noted once

`morning` is a fifth workspace of the same shape and spawns `gh`, `td` and whatever else its config
names, all keeps by the same reasoning as pns's own `gh` row. Its `capture` function is also the cleanest
existing model for the lights replacement above.

## Proposed slices

One tool per slice, one replacement group per slice, ordered by value. Each is small enough for one pull
request.

1. **posture: the pgrep family becomes one libproc walk.** The `-P 1 -x osqueryd` parent read and the two
   `-x -U <uid> <name>` agent reads, through `proc_listpids` plus `PROC_PIDTBSDINFO`, with posture's own
   copy of an interruptible wrapper (it may not borrow pns's). Three spawns of about 26 ms each leave the
   tick. Carries the `pbi_name` selection caveat the 2026-09-17 evaluation recorded, so the acceptance
   gate is agreement with `pgrep` on a live osqueryd and a live OverSight.
1. **posture: read property lists in process.** Replaces both `plutil` shapes and, on top of the same
   reader, the `defaults read` of the login window domain. Three commands, several spawns per tick, and
   the LuLu profile reading stops being a grep over rendered XML.
1. **posture: the four one-call replacements.** `kill -0` to `libc::kill`, `readlink -f` to
   `std::fs::canonicalize`, `file` to a four-byte magic read, `xattr -p` to `libc::getxattr`. No
   framework, no new dependency, and the only unsafe blocks are two libc calls.
1. **lights: bound the `pns` spawn in process and drop `gtimeout`.** Removes a PATH-resolved Homebrew
   dependency from a product installed with `cargo install`, which is a correctness fix as much as a
   spawn saved.
1. **uu: give the `CommandRunner` port an environment.** Drops `/usr/bin/env` from two lanes and
   `/bin/sh -c` from the npm lane, and takes the PATH-composition shell text with it.
1. **posture: the `-fq` osqueryd liveness read.** Held out of slice 1 because `-f` matches the whole
   command line and the native answer matches the executable path; the slice's first job is pinning which
   predicate the control actually means, then `proc_pidpath` or `KERN_PROCARGS2`.
1. **posture: evaluate `codesign` against Security.framework.** A measurement and a record-shape decision
   before any code: the framework calls are verified to exist, but the enrichment stores `codesign`'s
   text today and a signing-information dictionary is a different record.
1. **posture: find out whether the guest account and FileVault have public answers.** A research slice,
   not a build. Both are keeps today on the strength of a header search finding nothing, which is the
   weakest evidence in this document.

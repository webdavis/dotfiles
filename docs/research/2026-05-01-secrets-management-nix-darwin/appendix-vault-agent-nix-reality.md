# Vault Agent on macOS + Nix — research findings (May 2026)

## Method

Read-only research only. WebFetch against `developer.hashicorp.com` and `github.com`, plus
`gh search code/repos`. No edits, no `chezmoi apply`, no installs. Verbatim quotes preserved where the
docs are dispositive; paraphrase noted where the WebFetch model summarized.

## Findings (JSON-ish list)

```jsonc
[
  {
    "id": 1,
    "topic": "What Vault Agent is",
    "finding": "Vault Agent is HashiCorp's client-side daemon that performs auto-auth (token acquisition + renewal), template rendering of secrets to disk, and client-side response caching of tokens and leases.",
    "evidence_quote": "\"Automatically authenticate to Vault and manage the token renewal process for locally-retrieved dynamic secrets.\" / \"Allows rendering of user-supplied templates by Vault Agent, using the token generated by the auto-auth step.\" / \"Allows client-side caching of responses containing newly created tokens and responses containing leased secrets generated off of these newly created tokens. The agent also manages the renewals of the cached tokens and leases.\"",
    "source_url": "https://developer.hashicorp.com/vault/docs/agent-and-proxy/agent",
    "confidence": 0.95
  },
  {
    "id": 2,
    "topic": "Auto-auth methods (full list)",
    "finding": "Vault Agent supports 13 auto-auth methods: AliCloud, AppRole, AWS, Azure, TLS Certificates, Cloud Foundry (CF), GCP, JWT, LDAP, Kerberos, Kubernetes, OCI, Token File. There is NO `oidc` method in this list.",
    "evidence_quote": "Method list rendered on the page: AliCloud, AppRole, AWS, Azure, Certificates, CF, GCP, JWT, Kerberos, Kubernetes, LDAP, OCI, Token File.",
    "source_url": "https://developer.hashicorp.com/vault/docs/agent-and-proxy/autoauth/methods",
    "confidence": 0.95
  },
  {
    "id": 3,
    "topic": "OIDC vs JWT distinction",
    "finding": "OIDC is explicitly documented as browser-based human authentication; JWT is the machine-friendly variant. OIDC is NOT an auto-auth method — it requires a web browser, which is incompatible with daemon auto-auth.",
    "evidence_quote": "\"The OIDC method allows authentication via a configured OIDC provider using the user's web browser.\" / \"Alternatively, a JWT can be provided directly. The JWT is cryptographically verified using locally-provided keys.\"",
    "source_url": "https://developer.hashicorp.com/vault/docs/auth/jwt",
    "confidence": 0.9
  },
  {
    "id": 4,
    "topic": "Token File auto-auth — bootstrap location and warning",
    "finding": "`token_file` reads a Vault token from a file on disk (`token_file_path`). HashiCorp explicitly warns it is for development only, never production, and must not be used for long-running agents.",
    "evidence_quote": "\"reads in an existing, valid Vault token from a file, and uses that token in lieu of authenticating itself.\" / \"This authentication method is tailored for the development experience, and to facilitate getting started with Vault Agent and Vault Proxy. Vault Agent and Vault Proxy should never be configured to use this auto-auth method in a production environment.\" / \"For long-running Agent or Proxy processes, we strongly recommend another auto-auth method, such that Agent and Proxy are issuing their own own authentication requests to Vault.\"",
    "source_url": "https://developer.hashicorp.com/vault/docs/agent-and-proxy/autoauth/methods/token_file",
    "confidence": 0.95
  },
  {
    "id": 5,
    "topic": "AppRole bootstrap — files on disk",
    "finding": "AppRole auto-auth requires `role_id_file_path` (required) and `secret_id_file_path` (required unless `bind_secret_id=false`). By default the agent DELETES the secret_id file after first read (`remove_secret_id_file_after_reading=true`). Bootstrap problem: SOMETHING must place those files on disk before the agent starts.",
    "evidence_quote": "\"role_id_file_path (string: required) - The path to the file with role ID\" / \"secret_id_file_path (string: optional) - The path to the file with secret ID. If not set,\" / \"remove_secret_id_file_after_reading (bool: optional, defaults to true)\" — \"the agent will delete the file\" containing the secret ID by default after reading.",
    "source_url": "https://developer.hashicorp.com/vault/docs/agent-and-proxy/autoauth/methods/approle",
    "confidence": 0.95
  },
  {
    "id": 6,
    "topic": "JWT auto-auth bootstrap",
    "finding": "JWT requires a `path` to a file containing the JWT and a `role`. Like AppRole, the file is removed after reading by default (`remove_jwt_after_reading`). Same bootstrap problem: a JWT must be present on disk before the agent runs.",
    "evidence_quote": "Required: \"path (string: required) - The location where the JWT file is stored on disk\", \"role (string: required) - The role name for authentication against Vault\". Default: removes JWT file after reading unless `remove_jwt_after_reading` is set false.",
    "source_url": "https://developer.hashicorp.com/vault/docs/agent-and-proxy/autoauth/methods/jwt",
    "confidence": 0.9
  },
  {
    "id": 7,
    "topic": "macOS launchd integration — official docs",
    "finding": "HashiCorp documents `winsvc` (Windows service) integration but DOES NOT publish a launchd plist or a `brew services` recipe for Vault Agent. The `agent-quick-start` tutorial only shows `vault agent -config=...` invocation and a `pid_file` option. macOS service management is the user's problem.",
    "evidence_quote": "winsvc page opens with \"Vault Agent can be run as a Windows service.\" — no cross-reference to macOS/launchd. The install-binary tutorial covers Homebrew install but \"There is no information on this page about running Vault as a service on macOS, launchd, brew services, or any non-Windows service management.\" Site-scoped Google search for \"site:developer.hashicorp.com vault agent launchd\" returned only the winsvc page plus generic install/CLI pages.",
    "source_url": "https://developer.hashicorp.com/vault/docs/agent-and-proxy/agent/winsvc",
    "confidence": 0.9
  },
  {
    "id": 8,
    "topic": "Homebrew vault formula and brew services",
    "finding": "`hashicorp/tap/vault` (currently 2.0.0) advertises `brew services start hashicorp/tap/vault` for the Vault SERVER, not for Vault Agent. There is no separate vault-agent formula. Running Agent under launchd is entirely DIY.",
    "evidence_quote": "`brew info hashicorp/tap/vault` Caveats: \"To start hashicorp/tap/vault now and restart at login: brew services start hashicorp/tap/vault — Or, if you don't want/need a background service you can just run: /opt/homebrew/Cellar/vault/2.0.0/bin/vault server -dev\".",
    "source_url": "https://github.com/hashicorp/homebrew-tap/blob/HEAD/Formula/vault.rb",
    "confidence": 0.9
  },
  {
    "id": 9,
    "topic": "nixpkgs vault-agent module exists — but NixOS only",
    "finding": "`nixos/modules/services/security/vault-agent.nix` exists in nixpkgs and exposes `services.vault-agent.instances`. It generates SYSTEMD service units (`systemd.services`, `wantedBy = [ \"multi-user.target\" ]`). It is NixOS-only; it produces no launchd integration on darwin.",
    "evidence_quote": "Path: nixos/modules/services/security/vault-agent.nix. Top-level: services.vault-agent.instances. Unit generation via systemd.services.* with ExecStart/ExecReload/Restart. Maintainers are NixOS contributors; no darwin guards in the implementation.",
    "source_url": "https://github.com/NixOS/nixpkgs/blob/master/nixos/modules/services/security/vault-agent.nix",
    "confidence": 0.9
  },
  {
    "id": 10,
    "topic": "nix-darwin has no vault-agent module",
    "finding": "The nix-darwin services directory contains modules for aerospace, github-runner, hercules-ci-agent, jankyborders, karabiner, postgresql, redis, skhd, tailscale, etc. — but NO vault-agent and no vault module of any kind. `gh search code 'launchd.user.agents.vault'`, `gh search code 'launchd.daemons vault' --filename '*.nix'`, and `gh search code 'vault-agent' --filename '*.plist'` ALL returned zero hits.",
    "evidence_quote": "Directory listing of nix-darwin/modules/services on master: aerospace, github-runner, hercules-ci-agent, jankyborders, karabiner-elements, khd, kwm, mail, monitoring, nextdns, nix-gc, nix-optimise, ofborg, postgresql, privoxy, redis, sketchybar, skhd, spacebar, synergy, yabai, plus individual files autossh/buildkite-agents/cachix-agent/chunkwm/dnscrypt-proxy/dnsmasq/emacs/eternal-terminal/gitlab-runner/ipfs/lorri/mopidy/netbird/nix-daemon/openssh/spotifyd/synapse-bt/tailscale/trezord/wg-quick. \"No, there is neither a vault-agent nor a vault module present in this directory listing.\"",
    "source_url": "https://github.com/nix-darwin/nix-darwin/tree/master/modules/services",
    "confidence": 0.95
  },
  {
    "id": 11,
    "topic": "Vault Agent + nix-darwin in the wild — not found",
    "finding": "Every nixpkgs-vault-agent consumer found via gh search is NixOS-on-Linux: NixOS/nixpkgs itself, input-output-hk/bitte, numtide/systemd-vaultd, starcraft66/infrastructure, usmcamp0811/dotfiles (NixOS), leoank/neusis (NixOS), nahsi/nixconfig (NixOS), shedrachokonofua/aether, ananjiani/nixos-config, lopter/clan-destiny. Examined files all use `systemd.services.vault-agent*`. No public dotfiles repo configuring Vault Agent under launchd via nix-darwin was found.",
    "evidence_quote": "usmcamp0811/dotfiles: `systemd.services` based, NixOS module. ananjiani/nixos-config/hosts/servers/erebor/configuration.nix: `systemd.services.vault-agent-default = { after = [ \"openbao.service\" ]; wants = [ \"openbao.service\" ]; };` — \"This configuration is NixOS-only.\"",
    "source_url": "https://github.com/search?q=%22services.vault-agent%22+extension%3Anix&type=code",
    "confidence": 0.85
  },
  {
    "id": 12,
    "topic": "KV v2 — versioning, soft delete, NO auto-rotation",
    "finding": "KV v2 provides versioning and soft-delete (recoverable) plus permanent destroy. KV v2 does NOT auto-rotate static secrets — versioning is what you get instead of rotation.",
    "evidence_quote": "\"soft deletes to make data inaccessible while allowing data recovery. When an entry is permanently deleted, Vault purges the underlying version data and marks the key metadata as destroyed.\" — Page makes no reference to auto-rotation.",
    "source_url": "https://developer.hashicorp.com/vault/docs/secrets/kv/kv-v2",
    "confidence": 0.85
  },
  {
    "id": 13,
    "topic": "Database secrets engine — actual auto-rotation",
    "finding": "The database secrets engine generates dynamic credentials per-request with a TTL (default 1h) and revokes them via Vault's lease system. Static roles can be configured with a rotation period or schedule. Setup: enable the engine, configure the connection (root creds + plugin), then create roles with SQL creation statements. After setup, the operator must rotate the root password so only Vault holds it.",
    "evidence_quote": "\"The database secrets engine generates database credentials dynamically based on configured roles.\" / \"every service is accessing the database with unique credentials.\" / \"Vault makes use of its own internal revocation system to ensure that users become invalid within a reasonable time of the lease expiring.\" / Static roles: \"stores and automatically rotates passwords for the associated database user based on a configurable period of time or rotation schedule.\" / Setup: \"vault secrets enable database\", configure connection, create roles. After setup, rotate root so \"the vault user is not accessible by any users other than Vault itself.\"",
    "source_url": "https://developer.hashicorp.com/vault/docs/secrets/databases",
    "confidence": 0.9
  },
  {
    "id": 14,
    "topic": "Audit logging — disabled by default, fail-closed",
    "finding": "Audit is disabled at init. Once enabled, Vault HMAC-SHA256s sensitive values, logs all API requests/responses, and FAILS CLOSED if no audit device is reachable. HashiCorp recommends ≥2 audit devices to avoid Vault outages caused by audit-target unavailability.",
    "evidence_quote": "\"With a small set of exceptions, Vault audit devices record all API requests and responses in detail.\" / \"When you initialize a new Vault cluster, auditing is disabled.\" / \"only writes a keyed hash (HMAC-SHA256) of most string values to audit logs to protect the confidentiality of potentially sensitive information.\" / \"if you have audit devices enabled and Vault cannot log information to at least one of the enabled devices, Vault refuses to service the corresponding API request.\"",
    "source_url": "https://developer.hashicorp.com/vault/docs/audit",
    "confidence": 0.95
  },
  {
    "id": 15,
    "topic": "Template rendering retry behavior on Vault unreachable",
    "finding": "On startup, Agent retries auto-auth with backoff if Vault is unreachable. `exit_on_retry_failure` causes Agent to die after retries are exhausted. The template page does NOT document offline-rendering or stale-cache behavior — when Vault is unreachable, templates are not refreshed.",
    "evidence_quote": "\"will attempt to acquire a Vault token using the configured auto-auth Method. On failure, it will back off for a short while (including some randomness to help prevent thundering herd scenarios) and retry.\" / \"This option configures Vault Agent to exit after it has exhausted its number of template retry attempts due to failures.\" / \"The page does not contain content discussing offline operation or cached behavior for templates specifically.\"",
    "source_url": "https://developer.hashicorp.com/vault/docs/agent-and-proxy/agent/template",
    "confidence": 0.85
  },
  {
    "id": 16,
    "topic": "Persistent cache — not a generic offline cache",
    "finding": "Vault Agent persistent cache restores tokens and leases from a previous Agent process — but the only documented `persist` type is `kubernetes`. On shutdown, renewals stop immediately and cannot be resumed. Docs do not address Vault unreachability behavior of the cache. This is NOT 'work offline with stale secrets' functionality.",
    "evidence_quote": "\"Vault Agent can restore tokens and leases from a persistent cache file created by a previous Vault Agent process.\" / \"when the agent is shut down, all the renewal operations are immediately terminated and there is no way for agent to resume renewals after the fact.\" / \"The documentation does not explicitly address how Vault Agent caching behaves when Vault becomes unreachable or offline.\"",
    "source_url": "https://developer.hashicorp.com/vault/docs/agent-and-proxy/agent/caching",
    "confidence": 0.85
  },
  {
    "id": 17,
    "topic": "HA recommendation and single-node Raft",
    "finding": "HashiCorp's HA page recommends Integrated Storage (Raft) as the default HA backend; HA does NOT increase scalability (storage is the bottleneck). The Raft tutorial says: ≥5 servers for production, odd numbers to avoid stalemates, and 'we strongly discourage single server deployments for production use due to the high risk of data loss.' For a homelab — this is non-production, but you accept the data-loss risk explicitly.",
    "evidence_quote": "\"HashiCorp recommends Vault Integrated Storage as the default HA backend for new deployments of Vault.\" / \"HA does not enable increased scalability. In general, the bottleneck of Vault is the data store itself, not Vault core.\" / \"For best performance, we recommended at least 5 servers for a standard production deployment to maintained a minimum failure tolerance of 2.\" / \"We strongly discourage single server deployments for production use due to the high risk of data loss during failure scenarios.\"",
    "source_url": "https://developer.hashicorp.com/vault/docs/concepts/ha",
    "confidence": 0.95
  },
  {
    "id": 18,
    "topic": "Server availability dependency for the Mac client",
    "finding": "If the homelab Vault is offline when the Mac applies its config or starts Agent: auto-auth retries with backoff (or exits with `exit_on_retry_failure`); template re-rendering does not occur; the persistent cache restores PRIOR tokens/leases but renewals don't survive Agent shutdown. Practical implication: the Mac's secrets pipeline becomes coupled to homelab-Vault uptime — there is no documented 'offline grace period' for Vault Agent.",
    "evidence_quote": "Synthesized from finding 15 + 16 quotes above.",
    "source_url": "https://developer.hashicorp.com/vault/docs/agent-and-proxy/agent",
    "confidence": 0.75
  }
]
```

## Synthesis (≈250 words)

Vault Agent on a Nix-managed Mac is **not** well-trodden ground in May 2026. The official HashiCorp docs
cover Windows service integration explicitly; macOS launchd is absent — no plist, no `brew services`
recipe for the agent, no tutorial. The `hashicorp/tap/vault` Homebrew formula's `brew services` hook runs
`vault server`, not `vault agent`.

On the Nix side, `services.vault-agent.instances` exists in **nixpkgs** but is NixOS-only — it emits
`systemd.services.*` units with no darwin codepath. nix-darwin's services tree contains no vault module
of any kind. Every public dotfiles consumer of the nixpkgs module I found targets NixOS-on-Linux
(usmcamp0811, leoank, nahsi, ananjiani, lopter, …). I could not find a single public nix-darwin dotfiles
repo running Vault Agent under launchd. That's a meaningful absence, not just a search failure.

Bootstrap is the unavoidable wart on any auth method: token_file is dev-only per HashiCorp; AppRole needs
role_id and secret_id files on disk, with secret_id deleted after first read; JWT is the same shape; OIDC
is interactive and not an auto-auth method at all. Something outside Vault Agent must place the bootstrap
credential — keychain, KeePassXC, an out-of-band copy, or a launchd-pre script.

For a homelab + workstation Vault, single-node Raft is what you'll actually deploy, and the docs
explicitly call that "strongly discouraged for production use due to the high risk of data loss." For a
homelab that's an acceptable trade-off, but it is your trade-off, not a HashiCorp recommendation. Audit
logging is off by default and fail-closed once enabled — operator burden is non-trivial. The Mac becomes
coupled to homelab uptime: docs don't promise an offline grace period for template rendering.

# Ignore GitHub/repo metadata:
README.md
.gitignore
assets/
docs/
private/

# Repo scripts & helpers:
justfile
.envrc
scripts/

# Ignore formatter/linter configs:
.shellcheckrc

# Nix flake files:
flake.nix
flake.lock

# Ignore Ruby/Bundler stuff:
Gemfile
Gemfile.lock
.bundle/
vendor/**
.vendor/**

# Failsafe so that tmp files generated by lint.sh don't get applied to HOME.
tmp.*

# Machine-specific/templates:
.bashrc_local_darwin
.bashrc_local_linux

{{- if eq .chezmoi.os "linux" -}}
.Brewfile
.config/yabai
{{- end -}}

[bluebubbles]
url = {{ (keepassxc "BlueBubbles :: Server Password (dresden)").URL | toToml }}
server_password = {{ (keepassxc "BlueBubbles :: Server Password (dresden)").Password | toToml }}

[unifi]
url = "https://192.168.1.1"
api_key = {{ (keepassxc "UniFi :: API Key (dresden-udr)").Password | toToml }}
# Set a PEM CA bundle here when the controller uses a private certificate authority.
ca_file = ""

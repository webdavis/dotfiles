#
# ~/.bash_profile
#
# This file is triggered when Bash is started as a login shell. such as when a program
# like ssh is used, or when using the `--login` option. Only login specific settings
# should go in this file.
#

[[ -f $HOME/.bashrc ]] && \. $HOME/.bashrc

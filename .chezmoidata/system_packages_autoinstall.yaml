packages:
  macos:
    homebrew:
      taps:
        - antoniorodr/memo
        - buo/cask-upgrade
        - eugene1g/safehouse
        - felixkratz/formulae
        - furkankly/tap
        - mediosz/tap
        - nikitabobko/tap
        - openai/tools
        - openhue/cli
        - oven-sh/bun
        - owenthereal/upterm
        - silverstein/tap
        - rjyo/moshi
        - steipete/tap
        - vjeantet/tap
        - xdevplatform/tap
        - yakitrak/yakitrak
      # Every third-party tap declared above is trusted up front so Homebrew can
      # load its formulae, casks, or commands under the HOMEBREW_REQUIRE_TAP_TRUST
      # gate. The pre-bundle loop in run_onchange_before_10-system-packages.sh.tmpl
      # runs `brew trust --tap` for each (covers command taps too). The gate is a
      # load-time check, so an untrusted tap only fails when brew must (re)load it
      #, on a fresh install or an upgrade, which is why trusting all of them up
      # front is what keeps `brew bundle` from tripping. Keep in sync with `taps:`.
      trusted_taps:
        - antoniorodr/memo
        - buo/cask-upgrade
        - eugene1g/safehouse
        - felixkratz/formulae
        - furkankly/tap
        - mediosz/tap
        - nikitabobko/tap
        - openai/tools
        - openhue/cli
        - oven-sh/bun
        - owenthereal/upterm
        - silverstein/tap
        - rjyo/moshi
        - steipete/tap
        - vjeantet/tap
        - xdevplatform/tap
        - yakitrak/yakitrak
      formulae:
        - act
        - actionlint
        - age
        - agent-safehouse
        - alerter
        - antoniorodr/memo/memo
        - atomicparsley
        - atuin
        - bash
        - bashunit
        - bash-completion@2
        - bash-preexec
        - bat
        - bat-extras
        - bore-cli
        - brightness
        - btop
        - ca-certificates
        - carapace
        - ccusage
        - chezmoi
        - cloudflared
        - cmake
        - cmake-docs
        - coreutils
        - croc
        - csvlens
        - curl
        - deno
        - difftastic
        - direnv
        - droast
        - dust
        - elixir
        - espeak-ng
        - eza
        - fd
        - felixkratz/formulae/borders
        - ffmpeg
        - fnm
        - furkankly/tap/zoetrope
        - fzf
        - gawk
        - gh
        - git
        - git-crypt
        - git-delta
        - git-filter-repo
        - git-sizer
        - gitleaks
        - glow
        - gnu-getopt
        - gnu-sed
        - gnupg
        - go
        - gogcli
        - grpcurl
        - himalaya
        - httpie
        - hurl
        - hyperfine
        - imagemagick
        - iperf3
        - jq
        - just
        - lazydocker
        - librsvg
        - luacheck
        - mas
        - maven
        - mosh
        - mtr
        - neovim
        - netcat
        - ngrep
        - nmap
        - obsidian-cli
        - ollama
        - onefetch
        - openai-whisper
        - openai/tools/softnet
        - openhue/cli/openhue-cli
        - openjdk
        - opus
        - oven-sh/bun/bun
        - p7zip
        - parallel
        - pgvector
        - pinentry-mac
        - pnpm
        - portaudio
        - procs
        - pv
        - restic
        - ripgrep
        - rjyo/moshi/moshi-hook
        - ruff
        - sd
        - sevenzip
        - shellcheck
        - shfmt
        - socat
        - starship
        - steipete/tap/peekaboo
        - steipete/tap/remindctl
        - steipete/tap/sag
        - stylua
        - swiftformat
        - swiftlint
        - switchaudio-osx
        - tailscale
        - taplo
        - tart
        - tealdeer
        - terminal-notifier
        - tokei
        - trash-cli
        - tree
        - treefmt
        - tuicr
        - uv
        - vapor
        - websocat
        - wget
        - wireshark
        - worktrunk
        - xcbeautify
        - xcode-build-server
        - xcodegen
        - xcp
        - xonsh
        - xz
        - yakitrak/yakitrak/notesmd-cli
        - yq
        - zig
        - zizmor
        - zls
        - zoxide
      casks:
        - affinity
        - apple-juice
        - arc
        - blender
        - bluebubbles
        - brave-browser
        - cardhop
        - chatgpt
        - claude
        - claude-code@latest
        - codex
        - codex-app
        - discord
        - docker-desktop
        - espanso
        - fantastical
        - firefox
        - fluidvoice
        - font-fira-code
        - font-fira-code-nerd-font
        - font-symbols-only-nerd-font
        - gcloud-cli
        - ghostty
        - gitbutler
        - google-chrome
        - karabiner-elements
        - keepassxc
        - lulu
        - lunar
        - mediosz/tap/swipeaerospace
        - microsoft-auto-update
        - microsoft-office
        - minutes
        - mitmproxy
        - nikitabobko/tap/aerospace
        - obs
        - obsidian
        - openemu
        - osquery
        - oversight
        - proton-drive
        - proton-mail
        - proton-mail-bridge
        - raspberry-pi-imager
        - raycast
        - reader
        - retroarch-metal
        - signal
        - slack
        - thaw
        - todoist-app
        - torguard
        - upterm
        - xurl
        - zen
        - zoom
      mas:
        - name: Actions
          id: "1_586_435_171"
        - name: DaVinci Resolve
          id: "571_213_070"
        - name: Strongbox
          id: "897_283_731"
        - name: TestFlight
          id: "899_247_664"
        - name: Xcode
          id: "497_799_835"
    # Third-party crates installed with `cargo install`, for tools that ship
    # on crates.io with no Homebrew formula. The four Rust tools THIS
    # repository builds are not here: their source lives in this checkout and
    # each has its own apply-time builder, with the install destination
    # declared in .chezmoidata/rust_tools.yaml.
    #
    # `version` is required rather than optional, because an unpinned
    # `cargo install` resolves to whatever crates.io calls latest on the day a
    # fresh machine is built, which is not a decision anyone made.
    cargo:
      # A launcher and account manager for Claude Code. Its herdr plugin
      # (declared under packages.herdr_plugins) reads this binary's output and
      # is pinned to the matching tag, so the two versions move TOGETHER or the
      # sidebar renders a format the installed binary no longer prints.
      - name: clauth
        version: "0.15.2"
    uv:
      - graphifyy
      - nano-pdf
      - pymobiledevice3
      - sherlock-project
      - whisply[mlx,app]
    # fnm-managed node plus the npm CLI tools that run on it. One node version
    # per group; bumping `node` reinstalls every package under the new version
    # at the next apply.
    fnm:
      - node: "24"
        packages:
          # Pinned: the npm `latest` tag still points at 6.0.0 while the three
          # harness plugin bundles and their versions.json are on 6.0.3, so an
          # unpinned install would leave the CLI two months behind the plugins
          # that call it. `npm ls -g <pkg>@<version>` is exact, so the guard
          # above reinstalls whenever the pin moves.
          #
          # -sdk, NOT the @a5c-ai/babysitter metapackage the README names: only
          # this package ships the `adapters-hooks` bin that every Claude Code
          # hook command invokes and the Codex hook runtime resolves. The two
          # packages both declare a `babysitter` bin and cannot coexist.
          - "@a5c-ai/babysitter-sdk@6.0.3"
          - "@clawdbot/lobster"
          - "@doist/todoist-cli"
          - "@earendil-works/pi-coding-agent"
          - "@elevenlabs/cli"
          - "@fission-ai/openspec"
          - "@google/gemini-cli"
          - "@mistweaverco/kulala-ls"
          - "@oliverames/mcp-server-for-ynab"
          - "@tobilu/qmd"
          - acpx
          - agentmail-cli
          - backpass
          - clawhub
          # Pinned: an overnight orchestrator that commits unattended, still
          # pre-1.0 and releasing weekly, so the version it runs under is a
          # deliberate choice rather than whatever `latest` points at that
          # night. `npm ls -g <pkg>@<version>` is exact, so the guard above
          # reinstalls whenever the pin moves.
          - "gnhf@0.1.49"
          - hyperframes
          - mcporter
  # Tools built from someone else's git repository rather than this one, and
  # installed into rust_tools.install_dir with `cargo install --git`. dam is
  # the task-and-calendar CLI the herdr-damnit and damnit.nvim plugins spawn;
  # bumping `ref` reinstalls it on the next apply.
  cargo_git_tools:
    - name: dam
      repo: webdavis/damnit
      package: damnit
      ref: bae8a19a6e268ce0a06b67620eb8b9b80c2eee2c

  # The herdr plugins this machine builds and LINKS rather than installing from
  # GitHub. `herdr plugin install` requires a herdr-plugin.toml committed in the
  # plugin repository; a linked plugin ships none, because its actions are a
  # function of this machine's own configuration rather than a file that could
  # be committed once for everybody. herdr-process's actions are one set per
  # declared process profile, and herdr-workspace-jump's are one jump per
  # declared workspace; each renders its manifest from this machine's config
  # via its own `generate` subcommand. The shared builder,
  # .chezmoiscripts/run_onchange_after_58, clones each pinned revision,
  # compiles it, generates its manifest and links the result.
  herdr_linked_plugins:
    - id: herdr-process
      repo: webdavis/herdr-process
      ref: c587dda54e2fa71722630ae53974101cdf76989f
    - id: herdr-workspace-jump
      repo: webdavis/herdr-workspace-jump
      ref: 37621d26fff3d10fbb719e231d0cd891e75358d1

  herdr_plugins:
    # One roster, two consumers: run_after_53 installs a missing plugin at apply
    # and reinstalls one whose `ref` no longer matches the revision herdr
    # recorded, and uu's config template renders it into the herdr lane that
    # refreshes the unpinned ones weekly. Every plugin this machine wants is
    # listed here except the two linked ones (see herdr_linked_plugins above).
    #
    # herdr-smart-nav is ours, in its own public repository so it installs by
    # the same convention as everybody else's. Its `ref` is the revision herdr
    # checks out and builds; herdr v1 has no `plugin update`, so bumping this
    # value makes the next apply reinstall the plugin, exactly as for a
    # third-party pin.
    - id: herdr-smart-nav
      repo: webdavis/herdr-smart-nav
      ref: b82c2cf94de97cd3c6746d93de5a8e8da9fd112f
    # The task pane. Its config lives at
    # dot_config/herdr/plugins/config/herdr-damnit/config.toml and names the
    # KeePassXC entry the token comes from; the plugin refuses to run until one
    # of its two token indirections resolves.
    - id: herdr-damnit
      repo: webdavis/herdr-damnit
      ref: 6654365727d37ea718bd39d664f3c258a7bbe423
    - id: worktrunk
      repo: devashish2203/herdr-worktrunk
    - id: tab-smart-rename
      repo: iurysza/herdr-tab-smart-rename
    - id: persiyanov.reviewr
      repo: persiyanov/herdr-reviewr
    - id: herdr-agent-quota
      repo: levi-qiao/herdr-agent-quota
    - id: herdr-bar
      repo: jeffarese/herdr-bar
    - id: chmarax.herdr-nvim
      repo: ChmaraX/herdr-nvim
    - id: annotate
      repo: plannotator/herdr-annotate
    - id: furkankly.zoetrope
      repo: furkankly/zoetrope/herdr-plugin
    # Pinned to the tag the `clauth` binary itself is pinned to in the cargo
    # lane: the plugin reads the binary's output, so the two move together or
    # the sidebar reads a format the installed binary no longer prints.
    - id: clauth
      repo: uwuclxdy/clauth/herdr-plugin
      ref: v0.15.2

[package]
name = "morning-adapters"
version.workspace = true
edition.workspace = true

[dependencies]
morning-domain = { path = "../morning-domain" }
toml = { version = "1.1.4", default-features = false, features = [
  "parse",
  "serde",
  "std",
] }
serde = { version = "1.0.228", features = ["derive"] }

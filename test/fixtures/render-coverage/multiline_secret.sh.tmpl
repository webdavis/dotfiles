#!/bin/bash
# Fixture (F7): a LEGAL Go-template action split across two lines, opening with
# `{{ keepassxc` and closing on the next line. A per-line extractor that
# requires `{{ ... }}` closed on one line sees no complete action and classifies
# the template safe. The conservative rule: a line carrying an UNTERMINATED
# action start (`{{` with no closing `}}` on the same line, outside quotes) is
# not safely coverable, so the file is excluded (over-exclusion is tolerable;
# admitting a secret template never is).
set -euo pipefail
token={{ keepassxc
"Fixture :: Example" }}
echo "$token"

#!/bin/bash
# Fixture (C-F8): TWO includeTemplate directives on ONE line, the SECRET partial
# first then the safe one. A greedy parser that captures only the LAST action
# sees just the safe partial and wrongly classifies this "covered"; parsing every
# action reaches the secret partial (verdict: excluded).
set -euo pipefail
{{ includeTemplate "partial_secret.tmpl" . }} {{ includeTemplate "safe_partial.tmpl" . }}
echo "done"

#!/bin/bash
# Fixture (C-F8): the reverse order -- the safe partial first then the SECRET one
# on ONE line. The other-order guard: parsing every action must reach the secret
# partial regardless of position (verdict: excluded).
set -euo pipefail
{{ includeTemplate "safe_partial.tmpl" . }} {{ includeTemplate "partial_secret.tmpl" . }}
echo "done"

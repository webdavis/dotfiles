#!/bin/bash
# Fixture (C-F8): a line that STARTS with a Go-template COMMENT action and THEN
# carries a real keepassxc action. The old (ded84e2) classifier's comment guard
# was LINE-ANCHORED (any line beginning with a comment action was suppressed
# wholesale), so the comment action must open the line for this fixture to pin
# the proven defect. The action-scoped detector must ignore the comment action
# individually and still see the secret call on the same line (verdict:
# excluded).
set -euo pipefail
{{/* harmless comment */}} token={{ keepassxc "Fixture :: Example" }}
echo "$token"

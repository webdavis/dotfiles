#!/bin/bash
# Fixture (C-F8): a line that STARTS with a Go-template COMMENT action and THEN
# carries a real keepassxc action. A whole-line comment guard wrongly excludes
# the keepassxc call (classifying it "covered"); the action-scoped detector must
# ignore the comment action individually and still see the secret call on the
# same line (verdict: excluded).
set -euo pipefail
token={{/* harmless comment */}} {{ keepassxc "Fixture :: Example" }}
echo "$token"
